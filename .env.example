# -------------------------------------------------------------------------
# Application Settings
# -------------------------------------------------------------------------
# The port the application runs on
PORT=8080

# Gin Mode: "debug" (for development) or "release" (for production)
GIN_MODE=debug

# Secret key for signing JWT tokens (Change this in production!)
JWT_SECRET=my-secret-key-123


# -------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -------------------------------------------------------------------------
DB_HOST=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bulkops
DB_PORT=5432


# -------------------------------------------------------------------------
# AWS / S3 Configuration
# -------------------------------------------------------------------------
# S3 Bucket name for storing imports and exports
S3_BUCKET=bulk-imports

# AWS Region (Use us-east-1 for LocalStack)
AWS_REGION=us-east-1

# AWS Credentials (can be dummy values for LocalStack)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# -------------------------------------------------------------------------
# LocalStack Configuration (Development Only)
# -------------------------------------------------------------------------
# If running via Docker Compose, use: http://localstack:4566
# If running Go locally on your machine, use: http://localhost:4566
# Comment this out if deploying to real AWS.
AWS_ENDPOINT=http://localstack:4566